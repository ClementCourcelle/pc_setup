# bootstrap
## Usage

```sh
git clone git@github.com:ClementCourcelle/bootstrap.git
cd bootstrap
make          # shows available targets
make all      # full setup: tools + dotfiles + GNOME settings
```

Or as a single command:

```sh
./setup.sh
```
